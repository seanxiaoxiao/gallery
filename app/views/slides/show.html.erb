<p id="notice"><%= notice %></p>

<div class="container">
  <!-- The file upload form used as target for the file upload widget -->
  <form id="fileupload" action="/slide/<%= @slide.id %>/upload_photo" method="POST" enctype="multipart/form-data">
    <%= render 'generic/image_upload_form' %>
  </form>
</div>

<%= render 'generic/image_upload_template' %>

<%= link_to 'Edit', edit_slide_path(@slide) %> |
<%= link_to 'Back', slides_path %>
